var _ = require('underscore');

var tasks = require('../../../config/data/task');

var taskUtil = exports.modules

taskUtil.generateTasks = function(tasks, cb) {
  if (_.isNull(tasks) {

  }
  else {
    
  }
  cb(tasks);
}

taskUtil.updateTask = function(task, cb) {
  cb(task);
}
